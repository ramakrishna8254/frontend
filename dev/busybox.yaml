apiVersion: apps/v1
kind: Deployment
metadata:
  name: busybox
spec:
  replicas: 1  
  selector:
    matchLabels:
      app: busybox-test
  template:
    metadata:
      labels:
        app: busybox-test
    spec:
      imagePullSecrets:
      - name: imagepull-docker
      containers:
      - name: busybox
        image: ramakrishna8254/week:v3
        imagePullPolicy: Always
        ports:
          - containerPort: 8081
---

apiVersion: v1
kind: Service
metadata:
  name: busybox-test
spec:
  ports:
  - port: 8081
    targetPort: 8081
  selector:
    app: busybox-test
  type: LoadBalancer
